name: Publish

on:
  push:
    branches:
      - "main"

permissions:
  id-token: write # Required for OIDC
  contents: write # Read for contents, write for pushing tags

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup
        uses: ./.github/actions/setup

      - name: Type check
        run: pnpm run tc

      - name: Lint
        run: pnpm run lint

      - name: Test
        run: pnpm run test
  publish:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v4

      - name: Setup
        uses: ./.github/actions/setup

      - name: List pnpm config
        run: pnpm config list

      - name: Build
        run: pnpm run prepublishOnly

      - name: Check working tree
        run: git status --porcelain && git diff --stat

      - name: Publish
        run: pnpm publish

      - name: Tag release
        run: |
          #!/bin/bash
          set -eux

          NAME="$(node -p "require('./package.json').author.name")"
          if [[ -z "${NAME}" ]]; then
            echo "author.name is not set in package.json"
            exit 1
          fi

          EMAIL="$(node -p "require('./package.json').author.email")"
          if [[ -z "${EMAIL}" ]]; then
            echo "author.email is not set in package.json"
            exit 1
          fi

          VERSION="$(node -p "require('./package.json').version")"
          if [[ -z "${VERSION}" ]]; then
            echo "version is not set in package.json"
            exit 1
          fi

          git fetch --tags
          if git tag | grep "${VERSION}"; then
            echo "'${VERSION}' already exists. No action taken."
            exit 0
          fi

          git tag -a "${VERSION}" -m "Release ${VERSION}"
          git push --follow-tags
